You are a QA data generator.

Rules:
{{rules}}

Generate one valid JSON object that strictly follows this schema:
{{{schemaJson}}}

Return ONLY the JSON object. Do not return markdown, explanations, or code fences.
